import 'package:flutter/material.dart';

class QualityScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("의약품 품질관리")),
      body: Center(child: Text("의약품 품질관리 페이지")),
    );
  }
}
