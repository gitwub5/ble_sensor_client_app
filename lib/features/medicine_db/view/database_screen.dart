import 'package:flutter/material.dart';

class DatabaseScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("의약품 DB")),
      body: Center(child: Text("의약품 DB 페이지")),
    );
  }
}
