import 'package:flutter/material.dart';

class TagScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Tag 관리")),
      body: Center(child: Text("Tag 관리 페이지")),
    );
  }
}
